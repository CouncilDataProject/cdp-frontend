export enum WHERE_OPERATORS {
  eq = "==",
  contains = "array-contains",
  contains_any = "array-contains-any",
  in = "in",
  gt = ">",
  lt = "<",
  gteq = ">=",
  lteq = "<=",
}

export enum ORDER_OPERATORS {
  asc = "asc",
  desc = "desc",
}
