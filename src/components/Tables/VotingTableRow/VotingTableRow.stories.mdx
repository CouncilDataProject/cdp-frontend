import { action } from "@storybook/addon-actions";
import { text, array, number } from "@storybook/addon-knobs";
import { Meta, Props, Story } from "@storybook/addon-docs/blocks";

import VotingTableRow from "./VotingTableRow";

<Meta title="Library/Tables/Rows" component={VotingTableRow} />

# VotingTableRow

### A single Row

<Props of={VotingTableRow} />

<Story name="Single Row">
  <VotingTableRow
    legislationName={text("Legislation name", "Example Legislation Name")}
    legislationLink={text("Link to legislative text", "https://www.google.com")}
    index={number("The index of the row", 0)}
    voteDecision={text("Was the councilor's vote in favor of the legislation", "Reject")}
    councilDecision={text("What was the council's decision?", "Approve")}
    legislationTags={array("Tags", ["bike", "example", "lanes", "rental"])}
    meetingDate={text("JS Date Contructable String", "03/26/2021")}
    meetingLink={text("Link to Meeting", "https://www.google.com")}
    committeeName={text("Committee Name", "Example Committee")}
  />
</Story>