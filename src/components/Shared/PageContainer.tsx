import styled from "@emotion/styled";

/**A flex container for pages that display cards - legislations, events */
const PageContainer = styled.div({
  display: "flex",
  flexDirection: "column",
  gap: 32,
});

export default PageContainer;
