import styled from "@emotion/styled";

import { fontSizes } from "../../styles/fonts";

/**An element to display the status of fetching cards. */
const FetchCardsStatus = styled.p({
  fontSize: fontSizes.font_size_6,
});

export default FetchCardsStatus;
